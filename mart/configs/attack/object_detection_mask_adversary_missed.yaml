defaults:
  - object_detection_mask_adversary
  - objective: object_detection_missed
  - gain: rcnn_class_background

max_iters: 100

perturber:
  optimizer:
    lr: 3

  initializer:
    constant: 127
