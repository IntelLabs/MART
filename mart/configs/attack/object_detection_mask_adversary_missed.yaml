defaults:
  - object_detection_mask_adversary
  - perturber/gain: rcnn_class_background
  - perturber/objective: object_detection_missed

max_iters: 100

perturber:
  initializer:
    constant: 127

  optimizer:
    lr: 3
