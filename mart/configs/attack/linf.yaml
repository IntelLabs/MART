defaults:
  - composer/perturber/projector/functions: lp
  - enforcer: default
  - enforcer/constraints: lp

composer:
  perturber:
    projector:
      functions:
        lp:
          p: ${......p}
          eps: ${......eps}

enforcer:
  constraints:
    lp:
      p: ${....p}
      eps: ${....eps}

p:
  _target_: builtins.float
  _args_: ["inf"]
eps: ???
