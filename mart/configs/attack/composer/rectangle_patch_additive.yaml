defaults:
  - default
  - functions:
      [
        rect_crop,
        rect_pad,
        rect_perspective_transform,
        mask,
        additive,
        fake_clamp,
      ]

functions:
  rect_crop:
    order: 0
  rect_pad:
    order: 1
  rect_perspective_transform:
    order: 2
  mask:
    order: 3
  additive:
    order: 4
  fake_clamp:
    order: 5
