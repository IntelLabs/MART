defaults:
  - adversary
  - transform: tuple_to_pixel_range_255
  - transform@untransform: tuple_to_pixel_range_1
  - perturber: default
  - perturber/initializer: constant
  - perturber/projector: mask_range
  - composer: overlay
  - /optimizer@optimizer: sgd
  - gain: rcnn_training_loss
  - gradient_modifier: sign
  - objective: zero_ap
  - enforcer: default
  - enforcer/constraints: [mask, pixel_range]
  # - /callbacks@callbacks: [progress_bar, image_visualizer]

# Make a 5-step attack for the demonstration purpose.
max_iters: 5

optimizer:
  lr: 55

perturber:
  initializer:
    constant: 127
